let pokesData = pokesAPI.data.results;

        //guardamos el objeto completo
        console.log(pokesData)
let usersData = users.data.data;

//imprimimos el email por forEach
usersData.forEach(user => {
    console.log(user.avatar)}
//guardamos el objeto dentro de data
        let pokesData = pokesAPI.data.results;

     
        //para iterar dentro de los parámetros del objeto
       /*  pokesData.forEach( datos => {
            console.log(datos.url);